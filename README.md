# easygrade-canvaslms-mcp
This repository contains an MCP (Model Context Protocol) server that automates common tasks in Canvas LMS using the official Canvas API. It gives LLMs a safe, structured way to perform routine operations on behalf of instructors, admins, or developers.
